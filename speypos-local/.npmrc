# node-gyp configuration
# Keep desktop defaults untouched, but define android_ndk_path so
# plain `npm install` works in Termux without extra env vars.
android_ndk_path=/data/data/com.termux/files/usr
